# requirements.txt — Kaggle 云端依赖
# ===========================================
# finprivacy_audit / kaggle_cloud 运行环境
# 用于 Kaggle Notebook GPU P100/T4×2 环境

# ── 核心深度学习框架 ──
torch>=2.1.0,<2.5.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ── Transformers 生态 ──
transformers>=4.36.0
datasets>=2.16.0
accelerate>=0.25.0
peft>=0.7.0              # LoRA / QLoRA 微调
trl>=0.7.0               # 强化学习微调 (RLHF)
bitsandbytes>=0.41.0     # 8-bit / 4-bit 量化 (Kaggle GPU 兼容)
sentencepiece>=0.1.99    # 分词器
tokenizers>=0.15.0       # 高性能分词

# ── 数据处理 ──
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pyarrow>=14.0.0

# ── 差分隐私 ──
opacus>=1.4.0            # Meta 差分隐私训练库 (PyTorch)
dp-transformers>=0.1.0   # HuggingFace + DP 训练

# ── 审计与攻击工具 ──
# 成员推断 / 属性推断攻击框架
# (部分需从源码安装, 见下方注释)

# ── 可视化 ──
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.17.0

# ── 加密与安全 ──
cryptography>=41.0.0
pycryptodome>=3.19.0

# ── 日志与监控 ──
tqdm>=4.66.0
wandb>=0.16.0            # 实验追踪 (可选)

# ── 工具 ──
python-dotenv>=1.0.0
click>=8.1.0
rich>=13.0.0             # 终端美化

# ── Kaggle 特定 (通常预装, 显式声明以防版本冲突) ──
# kaggle>=1.6.0

# ── 可选: 成员推断攻击库 ──
# 如需完整 MIA 工具链, 可在 Kaggle 中运行:
# !pip install git+https://github.com/privacytrustlab/ml_privacy_meter.git
# !pip install git+https://github.com/tensorflow/privacy.git  (TF Privacy)
